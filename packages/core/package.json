{
  "name": "@uniforma/core",
  "version": "0.0.0",
  "description": "Core schema and validation utilities for Uniforma.",
  "license": "MIT",
  "files": [
    "src",
    "dist"
  ],
  "type": "module",
  "module": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "vp pack",
    "dev": "vp pack --watch",
    "test": "vp test",
    "check": "vp check",
    "prepublishOnly": "vp run build"
  },
  "dependencies": {
    "@standard-schema/spec": "catalog:",
    "json-pointer": "catalog:"
  },
  "devDependencies": {
    "@types/json-pointer": "catalog:",
    "zod": "catalog:"
  }
}
